# canopy/url — Features

- [ ] URL mutation helpers: `setPath`, `setQuery`, `setFragment`, `setHost`, `setPort`
- [ ] `origin : Url -> String` — Extract origin
- [ ] Path normalization (remove `.` and `..` segments)
- [ ] `addQueryParam : String -> String -> Url -> Url` — Add query parameter
- [ ] `removeQueryParam : String -> Url -> Url` — Remove query parameter
- [ ] `getQueryParam : String -> Url -> Maybe String` — Get single query parameter
